# Exercise: Neural Architecture Search

**Task:** Search over small model configs. **Req:** budgeted search, Pareto frontier. **Deliver:** best models under latency/size constraints. **Bonus:** evolutionary + early stopping.

## Setup

```bash
pip install -r requirements.txt
python main.py
```

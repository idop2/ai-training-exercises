# Exercise: Logit Processor

**Task:** Implement temperature, top-p, repetition penalty, banned tokens pipeline. **Req:** pluggable processor chain + tests. **Deliver:** reproducible sampling demo. **Bonus:** “JSON-friendly” sampling mode.

## Setup

```bash
pip install -r requirements.txt
python main.py
```

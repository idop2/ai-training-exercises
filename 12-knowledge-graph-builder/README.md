# Exercise: Knowledge Graph builder

**Task:** Extract entities/relations into triples w/ provenance. **Req:** store + query (`neighbors`, `paths`). **Deliver:** demo dataset + 10 queries. **Bonus:** confidence scores + dedup entities.

## Setup

```bash
pip install -r requirements.txt
python main.py
```

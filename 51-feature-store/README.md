# Exercise: Feature Store

**Task:** Build offline+online feature store. **Req:** point-in-time correctness, backfills, versioning. **Deliver:** training/serving consistency demo. **Bonus:** monitoring for feature drift.

## Setup

```bash
pip install -r requirements.txt
python main.py
```

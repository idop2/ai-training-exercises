# Exercise: Synthetic data generator

**Task:** Generate labeled tasks + validators. **Req:** acceptance filters, error taxonomy, provenance. **Deliver:** dataset + quality report. **Bonus:** feedback loop improves generator prompts.

## Setup

```bash
pip install -r requirements.txt
python main.py
```

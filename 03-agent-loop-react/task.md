# Exercise: Agent loop (ReAct)

**Task:** Implement agent loop with tools (`search`, `calc`, `doc_lookup`). **Req:** step budget, tool budget, structured trace log. **Deliver:** run logs + replay mode. **Bonus:** deterministic mode (fixed seed + cached tool outputs).
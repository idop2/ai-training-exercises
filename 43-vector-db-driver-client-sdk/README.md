# Exercise: Vector DB driver (client SDK)

**Task:** Build client library with retries/pooling. **Req:** CRUD + query + filters. **Deliver:** docs + examples + integration tests. **Bonus:** multi-language bindings.

## Setup

```bash
pip install -r requirements.txt
python main.py
```

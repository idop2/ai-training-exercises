# Exercise: Quantization library (int8 / fp4-ish)

**Task:** Implement int8 quant for linear layers + calibration. **Req:** accuracy/perplexity evaluation. **Deliver:** speed/memory results. **Bonus:** mixed precision + per-channel quant.

## Setup

```bash
pip install -r requirements.txt
python main.py
```

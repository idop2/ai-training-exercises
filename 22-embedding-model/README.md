# Exercise: Embedding model

**Task:** Train contrastive embedding model on pairs. **Req:** evaluation on retrieval set. **Deliver:** recall@k improvements vs baseline. **Bonus:** hard negative mining.

## Setup

```bash
pip install -r requirements.txt
python main.py
```

# Exercise: PEFT library

**Task:** Build mini library for LoRA/adapters/prefix. **Req:** unified API. **Deliver:** docs + examples. **Bonus:** export to common formats.

## Setup

```bash
pip install -r requirements.txt
python main.py
```

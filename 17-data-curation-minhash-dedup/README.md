# Exercise: Data curation (MinHash / dedup)

**Task:** Deduplicate near-duplicates at scale. **Req:** shingling + MinHash + LSH buckets. **Deliver:** dataset shrink %, collision stats. **Bonus:** keep best canonical doc by heuristic.

## Setup

```bash
pip install -r requirements.txt
python main.py
```

# Exercise: Semantic Router

**Task:** Route queries to prompt/tool/model based on embeddings. **Req:** thresholds + fallback. **Deliver:** confusion matrix over 100 labeled queries. **Bonus:** dynamic route learning (update exemplars).

## Setup

```bash
pip install -r requirements.txt
python main.py
```

# Exercise: Guardrails (I/O filtering + tool gating)

**Task:** Add a policy layer that blocks risky inputs/outputs and high-impact tool calls. **Req:** rule-based + classifier hook, allow/deny reasons, audit log. **Deliver:** adversarial test prompts. **Bonus:** “policy simulation” mode for debugging.
# Exercise: Prompt caching mechanism

**Task:** Cache repeated prompt prefixes and reuse results. **Req:** stable hash, TTL, invalidation, metrics. **Deliver:** before/after latency+cost report. **Bonus:** segment cache by tenant + permissions.

## Setup

```bash
pip install -r requirements.txt
python main.py
```

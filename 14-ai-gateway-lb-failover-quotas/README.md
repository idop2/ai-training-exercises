# Exercise: AI Gateway (LB / failover / quotas)

**Task:** Build gateway in front of multiple model endpoints. **Req:** routing rules, retries, circuit breaker, rate limits, metrics. **Deliver:** chaos test (simulate provider outage). **Bonus:** per-tenant budgets + audit trail.

## Setup

```bash
pip install -r requirements.txt
python main.py
```

# Exercise: Diffusion model (UNet + scheduler)

**Task:** Train DDPM on small dataset. **Req:** scheduler + sampling. **Deliver:** sample grid + FID-ish proxy. **Bonus:** DDIM sampling speedup.

## Setup

```bash
pip install -r requirements.txt
python main.py
```

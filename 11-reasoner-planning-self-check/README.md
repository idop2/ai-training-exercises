# Exercise: Reasoner (planning + self-check)

**Task:** Implement `reason()` producing plan, steps, final answer, checks. **Req:** verifiers (constraints, arithmetic, consistency). **Deliver:** 20 tasks + pass/fail report. **Bonus:** “ask for missing info” policy.

## Setup

```bash
pip install -r requirements.txt
python main.py
```

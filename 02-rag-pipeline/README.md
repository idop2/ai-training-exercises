# Exercise: RAG pipeline

**Task:** Implement chunk→embed→index→retrieve→answer. **Req:** citations (chunk IDs), “I don’t know” threshold, top-k tuning. **Deliver:** CLI + minimal web UI, 30-question test set + metrics. **Bonus:** answer verifier that checks citations contain keywords.

## Setup

```bash
pip install -r requirements.txt
python main.py
```

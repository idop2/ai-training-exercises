# Exercise: Matrix multiplication kernel

**Task:** Implement tiled GEMM (CPU or CUDA). **Req:** benchmark vs BLAS/cuBLAS. **Deliver:** roofline-ish analysis. **Bonus:** fused epilogue (bias+relu).

## Setup

```bash
pip install -r requirements.txt
python main.py
```

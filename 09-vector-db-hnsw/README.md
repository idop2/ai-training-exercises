# Exercise: Vector DB (HNSW)

**Task:** Implement HNSW index with add/query/persist. **Req:** recall@k benchmark vs brute force. **Deliver:** perf graphs + design doc. **Bonus:** deletions + filtered search.

## Setup

```bash
pip install -r requirements.txt
python main.py
```

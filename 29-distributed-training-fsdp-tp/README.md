# Exercise: Distributed training (FSDP / TP)

**Task:** Train model with sharding/parallelism. **Req:** memory vs throughput report, checkpointing. **Deliver:** reproducible multi-GPU run. **Bonus:** fault-tolerant resume.

## Setup

```bash
pip install -r requirements.txt
python main.py
```
